# Run the cohort simulation + analysis pipeline
# Usage (in R): source("scripts/run.R")
#
# Tip: If you do NOT want to auto-install packages, comment out the install section
# inside R/cohort_simulation_framework.R.

source(file.path("R", "cohort_simulation_framework.R"))
